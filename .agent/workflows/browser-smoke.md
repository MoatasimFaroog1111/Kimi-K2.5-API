---
description: Validate the Kimi-style workspace in a real Chromium browser
---

// turbo-all
1. Start the FastAPI application locally with non-production test credentials.
2. Open the workspace in Chromium at a desktop viewport and verify the authentication shell renders.
3. Verify chat/agent mode controls, model selector, composer, and sidebar exist.
4. Repeat the critical shell checks at a mobile viewport.
5. Save a smoke-test screenshot as a workflow artifact.
